# ~/.aliases
if [ -f ~/.aliases ]; then
  source ~/.aliases;
fi

# ~/.bashrc
if [ -f ~/.bashrc ]; then
  source ~/.bashrc;
fi

# ~/.environment
if [ -f ~/.environment ]; then
  source ~/.environment;
fi

# ~/.profile
if [ -f ~/.profile ]; then
  source ~/.profile;
fi
